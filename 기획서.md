# 프로젝트 기준선

## 문서 정보

- 프로젝트명: javis
- 문서 버전: v0.9
- 작성일: 2026-08-06
- 현재 상태: 구현 진행 중 (로컬에만 반영, 프로덕션 배포는 보류) — 업로드(자동 태깅·다중 파일)·현장사진 업무일지·Google Sheets 웹훅·자연어 검색·연관검색·AI 브리핑·전체 문서 보기·로그인까지 구현 (이전 기획 `jarvis-personal-ai-os`를 2일 규모로 축소, v0.1의 "기억 기반 대화" 방향에서 "문서 검색·전달"로 전환)

## 프로젝트 한 문장

여러 문서(자료, 노트, PDF 등)를 만들어 보관하는 나(개발자)가
나중에 필요할 때 그 문서를 어디에 저장했는지 기억나지 않아 폴더를 하나씩 뒤져야 하는 문제를
찾고 싶은 문서를 자연어로 설명하면 AI가 저장된 문서 중에서 찾아 
열람·다운로드 링크로 보여주는 방식으로 줄여 
휴대폰에서도 그 링크로 바로 문서를 확인할 수 있는 것을 확인할 수 있게 만듭니다.
 
## 1. 프로젝트 목적

이전 기획(`jarvis-personal-ai-os`)의 "사용자를 대신해 실제로 도움이 되는 일을 처리하는 AI"라는 방향은 유지하되, 도구 제어·안전 승인 같은 큰 구조 대신 "내가 저장해둔 문서를 자연어로 찾아준다"는 구체적이고 검증 가능한 업무 하나로 좁힌다. 검색 결과는 휴대폰에서도 바로 열어보거나 받을 수 있어야 한다.

## 2. 문제 정의

- 사용자: 나(개발자) 자신 — 여러 문서/자료를 만들고 보관하는 1인 사용자
- 사용 상황: 예전에 만들어 둔 문서가 나중에 다시 필요할 때, 파일명이나 저장 위치를 정확히 기억하지 못함
- 사용 상황(현장): 현장에 나가 있는데 필요한 도면이 손에 없으면(PC 앞이 아니라서 폴더를 뒤질 수도 없음) 그 자리에서 확인이 안 돼 난처한 상황이 됨
- 문제: 폴더와 파일명을 하나씩 뒤져야 해서 시간이 걸리고, 특히 휴대폰에서는 더 찾기 어려움
- 기대 변화: "그때 쓴 ~ 관련 문서" 처럼 자연어로 설명하면 AI가 후보를 찾아주고, 휴대폰에서도 바로 열람·다운로드할 수 있음. 현장에서도 휴대폰으로 웹에 접속해 필요한 도면을 즉시 검색·다운로드할 수 있어, 도면이 없어 난처해지는 상황 자체를 줄여줌

## 3. 프로젝트 범위 초안

### 이번 프로젝트에서 다룰 내용
- 문서 업로드 화면: 여러 파일을 한꺼번에 선택 → 파일마다 AI가 파일명(이미지는 3MB 이하일 때 실제 이미지 내용까지)을 보고 현장·구역명·설명을 자동으로 채움 → 사용자가 확인·수정 후 한 번에 업로드. 파일은 Supabase Storage에, 메타데이터는 documents 테이블에 저장
- 현장사진 업무일지 업로드: 일반 문서 업로드와 별도 영역에서 현장사진 여러 장을 선택 → Gemini가 사진마다 `위치`, `업무내용`, `자동 태그`를 추론 → 사용자가 확인·수정 후 반영하면 사진은 Supabase Storage/documents에 저장되고, 업무일지 행은 `work_logs` 테이블과 Google Sheets `업무일지` 탭에 기록됨. Google Sheets에는 `날짜`, `시간`, `위치`, `업무내용`, `태그`, `파일명`, `문서ID` 컬럼으로 기록
- 검색 화면 (홈): 찾고 싶은 문서를 자연어로 설명 입력 → Gemini가 이를 검색 키워드/조건으로 변환 → 저장된 문서 메타데이터 중 일치하는 항목을 찾아 결과로 표시
- 연관검색 (홈, 자연어 검색과 별도 입력창): "201동" 처럼 짧은 현장·구역명/키워드만 입력하면, 먼저 다른 현장으로 명확히 태깅된 문서만 제외해 범위를 좁히고, 그 안에서 Gemini가 이 현장에서 보통 필요한 문서 종류(체크리스트, 검측요청서 등)를 추론해 한 줄 코멘트로 보여주고 관련 문서를 찾아 보여줌. 일반 검색보다 현장 단위로 좁혀서 업무 관점으로 추천하는 용도
- 검색 결과 화면: 찾은 문서 목록과 각 문서의 열람·다운로드 링크 제공 (휴대폰 브라우저에서도 동일하게 열림/다운로드됨)
- AI 브리핑 (검색 결과의 각 문서): 파일을 열지 않고도 버튼 한 번으로 내용을 요약해서 보여줌. PDF·이미지는 Gemini가 원본을 직접 읽어 요약, HWP는 본문 텍스트를 추출해 요약(표·그림 위주 문서는 "지원 안 됨"으로 안내). 스프레드시트(xlsx 등)는 계좌번호·급여 같은 개인정보가 표 형태로 그대로 있는 경우가 많아 브리핑 대상에서 제외
- 전체 문서 보기 (/browse): 검색 없이 저장된 문서 전체를 현장·구역명별로 묶어서 목록으로 확인. 실제 폴더 구조는 저장하지 않으므로 site_name을 폴더처럼 사용(없으면 "미분류")
- 로그인: Google OAuth(Supabase Auth) + 이메일/비밀번호 테스트 계정. 로그인하지 않으면 모든 화면과 API가 `/login`으로 막힘. Supabase RLS도 `auth.uid() is not null`을 요구하도록 강화해서, 로그인 화면을 우회해 anon key로 직접 API/Storage를 호출해도 데이터가 나오지 않도록 함. Storage 버킷도 비공개로 전환하고, 다운로드 링크는 10분 뒤 만료되는 서명된 URL로 대체

### 이번 프로젝트에서 다루지 않을 내용 (원래 기획·초기 방향에서 제외)
- 이메일·문자·카카오톡 등으로 실제 "전송"하는 기능 — 웹 링크로 열람/다운로드하는 방식으로 대체. 전송 연동을 붙이면 별도 API/인증까지 늘어나 이틀 목표를 넘어서기 때문
- 문서 본문에 대한 OCR·전문 검색 — 제목·설명 등 메타데이터 기반 검색만 지원. 문서 내용 전체 검색(임베딩 등)은 정확도 튜닝에 시간이 걸려 다음 버전으로 미룸
- 문서 변환/포맷 변경 기능 (다운로드 가능한 HWP → PDF 파일 변환) — LibreOffice 같은 변환기가 필요한데 Vercel 서버리스 환경(읽기 전용 파일시스템, 실행시간 제한)에서는 실행할 수 없고, 유료 외부 변환 API를 붙이면 API 키·비용·요청 제한 관리까지 늘어나 이틀 목표를 넘어섬. 원본 파일 그대로 열람/다운로드만 제공.
  (단, AI 브리핑을 위한 HWP 본문 "텍스트 추출"은 `hwp.js`로 부분 지원함 — 이건 파일을 변환해서 내려주는 게 아니라 요약용 텍스트만 뽑는 것이라 별개 기능. `hwp.js`는 2020년 이후 업데이트가 없는 실험적 라이브러리라 표·그림 위주 문서에서는 텍스트가 비어 나올 수 있음)
- 멀티유저 권한 분리(사용자별로 문서를 나눠서 보이게 하는 것) — 로그인은 추가했지만, 로그인한 사람은 누구나 같은 문서 전체를 봄 (1인/소규모 팀 공용 도구 전제)
- 대용량 파일 지원 — Supabase Free 플랜의 File Storage가 1GB로 제한되어 있어(2026-08-06 확인), 개인용 소규모 문서 위주로 범위를 한정
- 실제 도구 제어(파일 삭제/이동 등 실행), 위험 승인 플로우, 음성 인터페이스, 선제적 알림, 감사 로그 시스템 — v0.1과 동일한 이유로 계속 제외

**줄인 이유:** "찾아서 보여주고 휴대폰에서 열 수 있게 한다"까지만 확실히 만들고, 실제 전송·본문 전문 검색·문서 변환처럼 범위를 넓히는 기능은 검증 가능한 핵심(자연어 → 검색 → 결과 확인)이 완성된 뒤 다음 버전에서 확장한다.

**향후 검토:** HWP → PDF 변환이 꼭 필요해지면, Vercel 서버리스가 아닌 별도 서버(LibreOffice 상주)나 유료 변환 API(CloudConvert 등) 연동을 다음 버전 범위로 검토한다.

## 4. 확인한 사실

- 이전 세션에서 작성한 `jarvis-personal-ai-os` 기획(SPEC.md, README.md)이 이미 존재하며, 이번 프로젝트는 그 축소판임
- 기술 스택은 Next.js / Vercel / Supabase / Gemini API로 고정
- Vercel 서버리스 함수는 요청 본문이 4.5MB로 제한되어 있어, API 라우트를 거치는 업로드 방식은 실제 크기의 문서(CAD 도면 PDF 등)에서 413 오류로 실패함을 확인 (2026-08-06). 브라우저에서 Supabase Storage로 직접 업로드하는 방식으로 전환해 해결
- Supabase Free 플랜 저장 용량: Database 500MB, File Storage 1GB (2026-08-06, supabase.com/pricing 확인)
- 로그인이 없고 Storage 버킷이 public이며 검색 API가 인증 없이 열려 있어서, 실제 회사 문서(계좌번호·급여 등 개인정보 포함)를 테스트로 올렸을 때 URL을 아는 누구나 검색·열람이 가능한 상태였음을 확인 (2026-08-06)
- 위 문제는 로그인(Google OAuth·이메일 로그인) 추가 + Supabase RLS를 `auth.uid() is not null` 요구로 강화 + Storage 버킷 비공개 전환 + 서명된 URL(10분 만료)로 해결함 (2026-08-06). 로그인 화면만 막는 것으로는 부족하고, anon key로 직접 API를 호출하는 경로까지 RLS로 막아야 실제로 안전하다는 것을 확인함
- AI 브리핑 기능은 별도로 스프레드시트(개인정보가 표로 그대로 노출되기 쉬움)를 대상에서 제외해 한 번 더 위험을 줄임
- Google Sheets 업무일지 연동은 Apps Script 웹앱 웹훅 방식으로 구현함 (2026-08-06). 웹앱 URL은 `.env.local`의 `GOOGLE_SHEETS_WEBHOOK_URL`로 관리하고, Apps Script 응답이 실제 `{ ok: true }`일 때만 성공 처리하도록 확인 로직을 추가함
- Apps Script 웹앱은 최상위 `function doPost(e)`만 인식함. `myFunction()` 안에 `doPost`를 중첩해 넣으면 웹훅 호출 시 `doPost를 찾을 수 없습니다` 오류가 발생함을 확인함

## 5. 아직 확인하지 않은 가정

- Gemini가 자연어로 표현된 검색 설명("그때 발표할 때 썼던 자료")을 실제 저장된 문서의 파일명/설명과 잘 매칭할 것이다 — 검증 방법: 다양한 표현으로 검색해 실제 원하는 문서가 상위에 나오는지 확인
- 메타데이터(파일명, 설명)만으로도 충분히 원하는 문서를 찾을 수 있을 것이다 — 검증 방법: 실제 문서 여러 개를 업로드하고 검색 정확도를 확인, 부족하면 본문 검색 필요성 재검토
- 현장사진에서 Gemini가 추론한 `위치`, `업무내용`, `태그`가 실제 업무일지 품질에 충분할 것이다 — 검증 방법: 실제 현장사진 여러 장으로 추론 결과를 확인하고, 필요한 경우 사람이 수정한 뒤 저장

## 6. 도메인 정의

사용자가 올려둔 문서를 저장해두고,
자연어로 찾는 문서를 설명하면 AI가 검색 조건으로 바꿔 일치하는 문서를 찾아
휴대폰을 포함한 어디서든 열람·다운로드할 수 있게 전달하는
개인 문서 검색 업무입니다.

### 도메인 요소

| 구분 | 도메인 요소 |
|---|---|
| 사람·역할 | 사용자 (1인) |
| 핵심 대상 | 문서(document), 검색 질의(query), 검색 결과(search_result) |
| 행동 | 문서 업로드, 문서 검색, 검색 결과 확인, 문서 열람/다운로드, 문서 삭제 |
| 규칙 | 검색은 문서의 파일명·설명 메타데이터를 기준으로 판단한다 / 삭제된 문서는 검색 결과에 나타나지 않는다 |
| 상태 | 업로드됨, 검색 중, 검색 완료, 삭제됨 |
| 이벤트 | 문서업로드됨, 검색요청됨, 검색결과생성됨, 문서열람됨, 문서삭제됨 |

**헷갈리기 쉬운 부분:** "검색"은 사용자의 행동(자연어 입력)이면서 동시에 AI가 수행하는 처리(키워드 변환 + 매칭)를 가리킵니다. 이 문서에서 "검색"은 기본적으로 **AI가 자연어 질의를 검색 조건으로 바꿔 문서를 찾는 처리 과정 전체**를 뜻합니다.

## 7. 사용자 역할

| 역할 | 책임 | 주요 행동 | 필요한 화면 | 접근 데이터 |
|---|---|---|---|---|
| 사용자 | 문서를 올리고, 필요할 때 찾아서 확인 | 문서 업로드, 자연어 검색, 결과 확인, 문서 열람/다운로드, 삭제 | 업로드, 검색(홈), 검색 결과 | 자신이 올린 문서 전체 |

## 8. 업무 흐름

### 정상 업무 흐름

1. 사용자가 업로드 화면에서 문서를 올리고 간단한 설명을 입력합니다.
2. 시스템이 파일을 Supabase Storage에 저장하고, 메타데이터를 documents 테이블에 저장합니다.
3. 사용자가 검색 화면에서 찾고 싶은 문서를 자연어로 설명합니다.
4. Gemini가 설명을 검색 키워드/조건으로 변환합니다.
5. 시스템이 저장된 문서 메타데이터 중 조건에 맞는 문서를 찾아 결과로 보여줍니다.
6. 사용자가 결과에서 문서를 선택해 열람하거나 다운로드합니다 (휴대폰에서도 동일).

### 현장사진 업무일지 흐름

1. 사용자가 업로드 화면 하단의 `현장사진 업무일지` 영역에서 사진 여러 장을 선택합니다.
2. 시스템이 각 사진을 Gemini에 보내 `위치`, `업무내용`, `자동 태그`를 추론합니다.
3. 사용자가 추론값을 확인하고 필요하면 수정합니다.
4. 사용자가 `현장사진 업무일지 반영`을 누릅니다.
5. 시스템이 사진 파일을 Supabase Storage에 저장하고, `documents` 테이블에 검색 가능한 사진 문서로 등록합니다.
6. 시스템이 `work_logs` 테이블에 업무일지 항목을 저장합니다.
7. 시스템이 `/api/work-log-sheet`를 통해 Google Apps Script 웹훅으로 같은 업무일지 데이터를 전송합니다.
8. Google Sheets의 `업무일지` 탭에 날짜·시간·위치·업무내용·태그·파일명·문서ID가 한 행으로 기록됩니다.
9. 기존 업무일지 양식에 업무내용이나 해당 내용을 가지고 와서 서류를 완성합니다.
### 예외 업무 흐름

**검색 결과 없음**
1. Gemini가 검색 조건을 만들었지만 일치하는 문서가 없습니다.
2. 화면에 "일치하는 문서가 없습니다"를 안내하고, 검색어를 바꿔보도록 제안합니다.

**AI 처리 실패**
1. Gemini API 호출이 실패하거나 시간이 초과됩니다.
2. 화면에 오류 안내를 표시합니다.
3. 기존에 저장된 문서는 영향을 받지 않습니다.

**빈 입력/업로드 실패**
1. 사용자가 검색어 없이 검색을 시도하거나, 파일 없이 업로드를 시도합니다.
2. API를 호출하지 않고 입력을 요청하는 안내를 표시합니다.

**Google Sheets 기록 실패**
1. 사진과 업무일지는 Supabase에 저장되었지만 Apps Script 웹훅이 실패하거나 `{ ok: true }`를 반환하지 않습니다.
2. 화면에 구글 시트 작성 실패를 안내합니다.
3. Apps Script 배포 버전, `doPost(e)` 위치, `GOOGLE_SHEETS_WEBHOOK_URL` 환경변수를 확인합니다.

## 9. 데이터 흐름과 아키텍처

**기능 한 문장:** 사용자가 파일을 올리면 AI가 파일명(또는 이미지 내용, DWG 도면이면 도면 안에서 추출한 텍스트)을 보고 현장명·설명을 자동으로 채워주고, 현장사진은 업무일지용 위치·업무내용·태그까지 추론해 시트에 기록하며(사진 없이 키워드만 입력해도 같은 방식으로 추론 가능), 찾는 문서를 자연어로 설명하면 AI가 이를 검색 조건으로 바꿔 저장된 문서 중 일치하는 것을 찾아 보여줍니다.

| 항목 | 내용 |
|---|---|
| 입력 데이터 | 검색 설명 텍스트 (검색 시), 파일 (업로드 시 — 설명·현장명은 비워둬도 됨, DWG 도면 포함), 현장사진 (업무일지 생성 시), 키워드 텍스트 (사진 없이 업무일지 생성 시) |
| AI가 참고하는 데이터 | (업로드) 파일명, 3MB 이하 이미지는 이미지 내용 자체, DWG 도면은 `dwgread`(LibreDWG)로 DXF 변환 후 뽑아낸 TEXT/MTEXT/ATTRIB 텍스트(표제란·시트제목·라벨, 최대 8000자) / (현장사진 업무일지) 이미지 내용 / (키워드 업무일지) 키워드 텍스트만(사진 없음) / (검색) 검색 설명 텍스트, 저장된 문서들의 파일명·설명·현장명 메타데이터 |
| AI 판단 기준 | (업로드) 파일명에서 현장·구역 패턴 추출, 파일명/이미지로 문서 종류 추정해 설명 문장 생성 — 근거 없으면 지어내지 않고 비워둠. DWG는 추출된 텍스트를 최우선 근거로 하고 부족하면 파일명을 보조로 사용 / (현장사진 업무일지) 사진에 보이는 공종·부재·점검 상황을 바탕으로 위치·업무내용·태그 추론 / (키워드 업무일지) 키워드에 위치 표현이 있으면 그대로 쓰고 없으면 키워드 자체를 위치로 삼아 업무내용·태그 추론 — 사진이 없으므로 확인 가능한 범위로만 작성 / (검색) 검색 설명에서 핵심 키워드·조건 추출, 문서 메타데이터와의 관련도 판단 |
| 출력 데이터 | (업로드) 현장·구역명, 설명 초안(사용자가 수정 가능, DWG도 동일한 형태로 출력) / (현장사진 업무일지) 위치, 업무내용, 태그(사용자가 수정 가능) / (키워드 업무일지) 위치, 업무내용, 태그(사용자가 수정 가능, 사진·문서 없이 생성) / (검색) 일치하는 문서 목록 (파일명, 설명, 열람 링크) |
| 저장할 데이터 | 문서(documents: 파일명, storage 경로, 설명, 현장·구역명, 업로드일 — DWG도 일반 업로드와 동일하게 저장), 업무일지(work_logs: 문서 id, 현장·구역명, 날짜, 내용 — 키워드 업무일지는 `document_id`가 null), Google Sheets 업무일지 행(날짜, 시간, 위치, 업무내용, 태그, 파일명, 문서ID — 키워드 업무일지는 파일명·문서ID가 비어 있음) |
| 오류 흐름 | 검색 결과 없음, 빈 입력, AI 응답 없음/실패, 업로드 실패 — 자동 태깅 실패 시에도 빈 값으로 두고 업로드는 계속 진행. DWG는 원본이 8MB를 넘거나 `dwgread` 변환이 20초 안에 끝나지 않거나 실패하면 파일명 기반 추론으로 자동 대체(업로드 자체는 막지 않음) |

### 구성요소

| 구성요소 | 역할 | 입력받는 정보 | 만들어 내는 결과 |
|---|---|---|---|
| 사용자 | 문서를 올리고(설명 없이도 됨, DWG 포함), 자연어 검색·연관검색·전체목록으로 결과를 확인하거나, 키워드만으로 업무일지를 기록 | 자동 채워진 현장명·설명, 검색·연관검색·전체목록 결과, 요약 브리핑 | 업로드 파일, 검색 설명, 연관검색 키워드, 업무일지 키워드 |
| 웹 클라이언트 (업로드/검색/결과 화면) | 입력을 받고 결과를 화면에 표시, 휴대폰 브라우저에서도 동일하게 동작. DWG는 추론 전에 Storage에 먼저 올려 storage_path만 서버에 전달 | 자동 태깅 결과, 검색·연관검색·전체목록 결과, 요약 브리핑 | API 호출 요청, Storage 직접 업로드(DWG 선업로드 포함) |
| Infer Metadata API | 파일명(또는 이미지, DWG storage 경로, 키워드)을 Gemini에 보내 현장명·설명 초안 또는 현장사진/키워드 업무일지 항목을 받아옴. DWG는 요청자가 해당 storage 경로의 소유자인지 확인 후 Storage에서 직접 다운로드해 처리 | 파일명, (이미지면) 이미지 데이터, (DWG면) storage 경로, (키워드 모드면) 키워드 텍스트(파일명 불필요) | 현장·구역명·설명 초안 또는 위치·업무내용·태그 |
| dwgread (LibreDWG 바이너리) | DWG 원본(8MB 이하)을 DXF로 변환(최대 20초 타임아웃, Vercel에서는 `bin/dwgread`, 로컬에서는 Homebrew PATH 사용) | DWG 파일 | DXF 파일 → TEXT/MTEXT/ATTRIB 텍스트(최대 8000자) |
| Work Log Sheet API | 현장사진/키워드 업무일지 데이터를 Google Apps Script 웹훅으로 전달 | 날짜, 시간, 위치, 업무내용, 태그, (있으면) 파일명, 문서ID | Google Sheets 기록 성공/실패 |
| Search API | 검색 설명과 저장된 문서 메타데이터를 Gemini로 비교해 일치 문서를 찾음 | 검색 설명 텍스트 | 일치 문서 목록 |
| Related Search API | 현장명·키워드로 문서를 1차 필터링한 뒤 Gemini에게 관련 문서와 추론 코멘트를 받아옴 | 현장명·키워드, 문서 메타데이터 목록 | 추론 코멘트, 관련 문서 목록 |
| Documents API | 저장된 문서 전체를 조회 (전체 문서 화면, 현장·구역별 묶음은 웹 클라이언트가 표시) | (없음, 전체 조회) | 문서 메타데이터 전체 목록 |
| Brief API | 문서 원본을 Storage에서 내려받아 Gemini로 요약 (PDF·이미지는 원본 그대로, HWP는 텍스트 추출 후) | document_id | 문서 요약 브리핑 |
| Gemini API | 파일명/이미지/DWG 추출 텍스트/업무일지 키워드로 현장명·설명·업무일지 항목 추론, 검색 설명·키워드와 문서 메타데이터를 비교해 일치 문서 판단, 문서 원본 요약 | 파일명·이미지·DWG 추출 텍스트, 검색 설명 또는 연관검색/업무일지 키워드, 문서 메타데이터 목록, (요약 시) 문서 원본 | 현장명·설명 초안, 위치·업무내용·태그, 일치 문서 목록, 추론 코멘트, 요약 브리핑 |
| Supabase Storage | 웹 클라이언트가 직접 파일을 저장(용량 제한 우회)하고 열람 링크 제공, 브리핑 시 서버가 파일 원본을 직접 다운로드 | 파일 | 서명된 파일 URL, 파일 원본 데이터 |
| Supabase (documents 테이블) | 문서 메타데이터 저장·조회 | 파일명, 설명, 현장명, storage 경로 | 문서 메타데이터 목록 |
| Supabase (work_logs 테이블) | 현장사진 또는 키워드에서 생성된 업무일지 항목 저장 (키워드 업무일지는 문서 id 없이 저장) | 문서 id(선택), 현장명, 날짜, 업무내용 | 업무일지 데이터 |
| Google Sheets (`업무일지` 탭) | 업무일지를 외부 표 형태로 누적 | 날짜, 시간, 위치, 업무내용, 태그, 파일명, 문서ID | 공유 가능한 업무일지 테이블 |

### Mermaid 데이터 흐름도

```mermaid
flowchart LR
    %% 문서 업로드 + AI 자동 태깅
    U[사용자] -->|파일 선택| WEB[웹 클라이언트]
    WEB -->|파일명/이미지| IAPI[Infer Metadata API]
    IAPI -->|파일명 또는 이미지| AI[Gemini API]
    AI -->|현장명·설명 초안| IAPI
    IAPI -->|자동 채움| WEB
    WEB -->|확인/수정 후 업로드| STG[(Supabase Storage)]
    WEB -->|메타데이터 저장| DB[(Supabase\ndocuments 테이블)]

    %% DWG 도면 자동 인식
    WEB -->|DWG 선업로드| STG
    WEB -->|파일명 + storage_path| IAPI
    IAPI -->|소유권 확인 후 다운로드 8MB 이하| STG
    STG -->|DWG 원본| IAPI
    IAPI -->|DWG→DXF 변환 요청 20초 타임아웃| DWGR[dwgread\nLibreDWG]
    DWGR -->|DXF| IAPI
    IAPI -->|추출 텍스트 또는 실패 시 파일명| AI
    AI -->|현장명·설명 초안| IAPI

    %% 현장사진 업무일지
    WEB -->|현장사진| IAPI
    AI -->|위치·업무내용·태그| IAPI
    WEB -->|업무일지 저장| WDB[(Supabase\nwork_logs 테이블)]
    WEB -->|시트 기록 요청| WAPI[Work Log Sheet API]
    WAPI -->|Apps Script 웹훅| SHEET[(Google Sheets\n업무일지 탭)]

    %% 키워드 업무일지 (사진 없음, documents 미생성)
    U -->|키워드만 입력| WEB
    WEB -->|키워드| IAPI
    IAPI -->|키워드| AI
    AI -->|위치·업무내용·태그| IAPI
    IAPI -->|자동 채움| WEB
    WEB -->|업무일지 저장 document_id null| WDB
    WEB -->|시트 기록 요청| WAPI

    %% 자연어 검색
    U -->|검색 설명 입력| WEB
    WEB -->|검색 요청| SAPI[Search API]
    SAPI -->|문서 메타데이터 조회| DB
    DB -->|문서 메타데이터 목록| SAPI
    SAPI -->|검색 설명 + 메타데이터 목록| AI
    AI -->|일치 문서 인덱스| SAPI
    SAPI -->|검색 결과 표시| WEB
    SAPI -.오류/결과없음.-> ERR[오류·안내]

    %% 연관검색 (현장명·키워드 기반 추론)
    U -->|현장명/키워드 입력| WEB
    WEB -->|연관검색 요청| RAPI[Related Search API]
    RAPI -->|문서 메타데이터 조회| DB
    DB -->|문서 메타데이터 목록| RAPI
    RAPI -->|키워드 + 후보 문서 목록| AI
    AI -->|추론 코멘트 + 일치 문서| RAPI
    RAPI -->|코멘트 + 결과 표시| WEB
    RAPI -.오류.-> ERR

    %% 전체 문서 목록
    U -->|전체 문서 화면 이동| WEB
    WEB -->|목록 요청| DAPI[Documents API]
    DAPI -->|문서 메타데이터 조회| DB
    DB -->|문서 메타데이터 목록| DAPI
    DAPI -->|문서 목록 응답| WEB
    DAPI -.오류.-> ERR

    %% AI 브리핑 (문서 요약)
    WEB -->|브리핑 요청 document_id| BAPI[Brief API]
    BAPI -->|문서 조회| DB
    DB -->|storage 경로 등| BAPI
    BAPI -->|파일 다운로드| STG
    STG -->|파일 데이터| BAPI
    BAPI -->|원본 또는 추출 텍스트| AI
    AI -->|요약 브리핑| BAPI
    BAPI -->|브리핑 표시| WEB
    BAPI -.오류/미지원.-> ERR

    WEB -->|열람/다운로드| STG
    ERR -.-> WEB
```

## 10. 데이터 구조 초안

### 10.1 문서 업로드 입력 데이터

| 데이터 항목 | 설명 | 필수 여부 |
|---|---|---|
| `file` | 업로드할 파일 | 필수 |
| `description` | 문서에 대한 설명 (검색에 쓰일 근거) | 선택 |

### 10.2 문서(documents) 데이터

| 데이터 항목 | 설명 | 필수 여부 |
|---|---|---|
| `document_id` | 문서 고유 번호 | 필수 |
| `file_name` | 원본 파일명 | 필수 |
| `storage_path` | Supabase Storage 내 저장 경로 (업로드 시 무작위 UUID로 생성, 원본 파일명의 한글·공백은 저장 키에 쓰지 않음) | 필수 |
| `description` | 문서 설명 | 선택 |
| `site_name` | 현장·구역명 (예: "201동") — 연관검색의 매칭 근거 | 선택 |
| `file_size` | 파일 크기(바이트) | 선택 |
| `uploaded_at` | 업로드 일시 | 필수 |

현장사진 업무일지로 업로드한 사진은 `description`에 업무내용과 태그를 함께 저장합니다. 현재 별도 `tags` 컬럼은 없으므로 `태그: 201동, 지하1층, 철근...` 형태의 텍스트가 검색 근거로 사용됩니다.

### 10.3 업무일지(work_logs) 데이터

| 데이터 항목 | 설명 | 필수 여부 |
|---|---|---|
| `document_id` | 연결된 현장사진 문서 id | 선택 |
| `site_name` | 위치 또는 현장·구역명 | 선택 |
| `log_date` | 업무일지 날짜 | 필수 |
| `content` | 업무내용 | 필수 |

### 10.4 Google Sheets 업무일지 행

| 데이터 항목 | 설명 | 필수 여부 |
|---|---|---|
| `date` | 한국 시간 기준 날짜 | 필수 |
| `time` | 한국 시간 기준 시각 (`HH:mm:ss`) | 필수 |
| `location` | 위치 (예: `201동 지하 1층`) | 필수 |
| `work_content` | 업무내용 | 필수 |
| `tags` | 쉼표로 구분되는 자동 태그 | 선택 |
| `file_name` | 원본 사진 파일명 | 필수 |
| `document_id` | Supabase documents의 문서 id | 선택 |

### 10.5 검색 질의·결과 데이터

| 데이터 항목 | 설명 | 필수 여부 |
|---|---|---|
| `search_id` | 검색 고유 번호 | 필수 |
| `query_text` | 사용자가 입력한 자연어 검색 설명 | 필수 |
| `matched_document_ids` | 검색된 문서 id 목록 | 선택 (결과 없음이면 빈 배열) |
| `result_count` | 검색 결과 수 | 필수 |
| `searched_at` | 검색 일시 | 필수 |

## 11. 데이터 소스

| 데이터 | 처음 발생하는 곳 | 사용하는 이유 |
|---|---|---|
| 업로드 파일, 설명 | 업로드 화면 | 문서 저장 및 검색 대상 등록 |
| 검색 설명 | 검색 화면 | 검색 조건 생성 |
| 검색 조건, 일치 문서 순위 | Gemini (검색 처리) | 검색 결과 판단 |
| 파일 URL | Supabase Storage | 열람·다운로드 제공 |
| 업무일지 행 | 현장사진 업무일지 업로드 화면, Gemini 추론, 사용자 수정 | `work_logs` 저장 및 Google Sheets 기록 |
| Google Sheets 웹훅 URL | `.env.local`의 `GOOGLE_SHEETS_WEBHOOK_URL` | 업무일지 행을 Google Sheets로 append |

## 12. 이벤트 카탈로그

| 이벤트 | 발생 조건 | 상태 변화 | 다음 처리 |
|---|---|---|---|
| `document_uploaded` | 파일 업로드가 완료됨 | 없음 → 업로드됨 | 검색 대상에 포함 |
| `site_photo_work_log_created` | 현장사진 업무일지 반영이 완료됨 | 없음 → 업무일지 생성됨 | `work_logs` 저장, Google Sheets 기록 |
| `work_log_sheet_write_failed` | Google Sheets 웹훅 호출 또는 응답 확인이 실패함 | 시트 기록 중 → 시트 기록 실패 | Apps Script 배포/URL 확인 안내 |
| `document_deleted` | 사용자가 문서 삭제를 요청함 | 업로드됨 → 삭제됨 | 검색 결과에서 제외 |
| `search_requested` | 사용자가 검색 설명을 제출함 | 없음 → 검색 중 | Gemini 검색 조건 생성 요청 |
| `search_completed` | 검색 결과가 생성됨 | 검색 중 → 검색 완료 | 결과 화면 표시 |
| `search_failed` | 검색 처리 중 오류 발생 | 검색 중 → 검색 실패 | 오류 안내 |

## 13. 용어와 네이밍 컨벤션

| 한글 공식 용어 | 영문 기준 이름 | 고유 번호 |
|---|---|---|
| 문서 | `document` | `document_id` |
| 검색 | `search` | `search_id` |

- 고유 번호는 이름 뒤에 `_id`를 붙입니다.
- 여러 영문 단어는 `snake_case`로 작성합니다.
- 이벤트는 이미 발생한 사건을 과거형으로 작성합니다 (`document_uploaded`, `search_completed`).
- 같은 대상은 문서·데이터·이벤트에서 같은 뿌리 단어(`document`, `search`)를 사용합니다.

## 14. MVP 범위와 기능 우선순위 (MoSCoW)

| 기능 | 우선순위 | 선택 이유 |
|---|---|---|
| 문서 업로드 | Must | 검색할 대상 문서가 없으면 아무것도 동작하지 않음 |
| 현장사진 업무일지 | Must | 현장사진을 업무일지 행으로 바꾸는 현재 핵심 확장 기능 |
| Google Sheets 업무일지 기록 | Must | 사용자가 요청한 최종 업무일지 테이블 산출물 |
| 설명 입력 | Must | 메타데이터 기반 검색의 유일한 근거 |
| 자연어 검색 | Must | 프로젝트의 핵심 AI 기능 |
| 검색 결과 목록 표시 | Must | 사용자가 결과를 확인해야 함 |
| 열람·다운로드 링크 | Must | 실제로 문서를 받아야 목적이 달성됨 |
| 검색 결과 없음 안내 | Must | 빈 결과에서도 화면이 깨지지 않아야 함 |
| 검색 기록 저장 | Should | 있으면 좋지만 핵심 검증에는 필수는 아님 |
| 문서 삭제 | Should | 편의 기능, 없어도 핵심 흐름에는 영향 없음 |
| 문서 본문 전문 검색(OCR) | Won't now | 범위가 커지고 정확도 튜닝에 시간이 필요함 |
| 이메일·문자 전송 | Won't now | 외부 연동이 늘어나 이틀 목표를 초과함 |
| 로그인 | Must | 실제 업무 문서 보안을 위해 필요 |
| 멀티유저 권한 분리 | Won't now | 로그인한 사용자는 같은 문서 전체를 보는 소규모 팀 공용 도구 전제 |

## 15. 완료 기준 (Definition of Done)

| 기능 | 완료 기준 |
|---|---|
| 문서 업로드 | 파일과 설명(선택)을 입력하고 업로드하면 `documents` 테이블에 레코드가 생성되고 Storage에 파일이 저장됩니다. |
| 현장사진 업무일지 | 현장사진 여러 장을 선택하면 사진마다 위치·업무내용·태그가 자동 추론되고, 사용자가 수정 후 반영할 수 있습니다. |
| Google Sheets 기록 | 현장사진 업무일지 반영 시 Google Sheets `업무일지` 탭에 날짜·시간·위치·업무내용·태그·파일명·문서ID가 기록됩니다. |
| 자연어 검색 | 검색창에 찾는 문서를 설명하고 검색을 실행하면, 관련도 높은 문서가 있을 경우 1개 이상 목록으로 표시됩니다. |
| 검색 결과 없음 | 조건에 맞는 문서가 없으면 "일치하는 문서가 없습니다" 안내가 표시됩니다. |
| 문서 열람·다운로드 | 검색 결과에서 문서를 선택하면 데스크톱과 휴대폰 브라우저 모두에서 파일이 열리거나 다운로드됩니다. |
| 빈 입력 처리 | 검색어 없이 검색하거나 파일 없이 업로드를 시도하면 API를 호출하지 않고 입력을 요청하는 안내가 표시됩니다. |
| AI 실패 처리 | Gemini 호출이 실패하거나 시간이 초과되면 화면이 깨지지 않고 오류 안내가 표시됩니다. |

## 16. 완료 체크리스트

### 프로젝트 목적과 범위
- [ ] 핵심 사용자와 해결하려는 문제가 작성되어 있습니다.
- [ ] 이번 프로젝트에서 만들 기능과 만들지 않을 기능이 구분되어 있습니다.
- [ ] MVP(Must) 기능과 이후 기능(Should/Won't now)이 구분되어 있습니다.

### 업로드·검색 기능
- [ ] 파일과 설명을 입력해 문서를 업로드할 수 있습니다.
- [ ] 현장사진 여러 장을 업로드해 위치·업무내용·태그를 자동 추론할 수 있습니다.
- [ ] 현장사진 업무일지 반영 시 `work_logs`와 Google Sheets에 기록됩니다.
- [ ] 자연어로 검색하면 관련 문서가 목록으로 표시됩니다.
- [ ] 일치하는 문서가 없을 때 안내가 표시됩니다.
- [ ] 검색 결과에서 문서를 열람·다운로드할 수 있습니다.
- [ ] 휴대폰 브라우저에서도 동일하게 동작합니다.
- [ ] 검색어/파일이 비어 있을 때 화면이 깨지지 않습니다.
- [ ] AI 호출이 실패해도 화면이 깨지지 않습니다.

### 문서
- [ ] 도메인 요소와 사용자 역할이 작성되어 있습니다.
- [ ] 정상 흐름과 예외 흐름이 작성되어 있습니다.
- [ ] 데이터 구조, 데이터 소스, 이벤트 카탈로그, 네이밍 규칙이 작성되어 있습니다.
- [ ] 데이터 흐름도와 아키텍처가 작성되어 있습니다.
- [ ] MVP 범위와 완료 기준이 작성되어 있습니다.

## 17. WBS (작업 분해)

6주가 아니라 **2일** 규모로 조정합니다. 담당자는 1인 개발이라 모두 "본인"입니다.

| WBS ID | 결과물·작업 | 담당자 | 예상 시간 | 선행 작업 | 완료 조건 |
|---|---|---|---:|---|---|
| 1.1 | Next.js 프로젝트 세팅 + Supabase 연결 | 본인 | 1시간 | 없음 | 로컬에서 Supabase 연결이 확인됩니다. |
| 1.2 | `documents` 테이블 + Storage 버킷 생성 | 본인 | 1시간 | 1.1 | 파일을 올리면 테이블에 레코드가 생성됩니다. |
| 1.3 | 업로드 화면 + Upload API 구현 | 본인 | 2시간 | 1.2 | 파일과 설명을 업로드하면 Storage·DB에 저장됩니다. |
| 1.4 | 검색 화면 UI(입력창, 결과 목록) 뼈대 작성 | 본인 | 1.5시간 | 1.1 | 정적 화면이 렌더링됩니다. |
| 2.1 | Gemini 연동: 검색 조건 변환 프롬프트 작성 | 본인 | 2시간 | 1.2 | 자연어 입력을 넣으면 검색 키워드/조건이 반환됩니다. |
| 2.2 | Search API 구현 (Gemini 결과로 documents 매칭) | 본인 | 2시간 | 2.1, 1.3 | 검색 요청 시 관련 문서 목록이 반환됩니다. |
| 2.3 | 검색 결과 화면 연결 + 열람·다운로드 링크 | 본인 | 1.5시간 | 2.2 | 결과를 클릭하면 파일이 열리거나 다운로드됩니다. |
| 2.4 | 예외 처리 (빈 입력, 결과 없음, AI 실패) | 본인 | 1시간 | 2.3 | 어떤 경우에도 화면이 깨지지 않고 안내가 표시됩니다. |
| 2.5 | 모바일 반응형 확인 + Vercel 배포 | 본인 | 1시간 | 2.4 | 시크릿 창·휴대폰에서 정상 동작이 확인됩니다. |
| 2.6 | 문서화(README, 스크린샷) + 최종 점검 | 본인 | 1시간 | 2.5 | 제출 체크리스트를 통과합니다. |

### 선행 작업 관계

```text
프로젝트 세팅
→ documents 테이블/Storage
→ 업로드 화면·API
→ Gemini 검색 조건 변환
→ Search API
→ 검색 결과·다운로드 화면
→ 예외 처리
→ 배포
→ 문서화
```

### 주요 기능의 인수 조건

**업로드**
- [ ] 파일과 설명을 입력해 업로드할 수 있습니다.
- [ ] 업로드 후 `documents` 테이블에 레코드가 생성됩니다.

**검색**
- [ ] 자연어로 검색하면 관련 문서가 1개 이상일 때 목록으로 표시됩니다.
- [ ] 일치하는 문서가 없으면 안내 문구가 표시됩니다.
- [ ] 검색 결과에서 문서를 열람·다운로드할 수 있습니다.

### 팀(1인) 완료의 정의

- [ ] 각 작업의 완료 조건을 만족합니다.
- [ ] 정상 흐름과 예외 흐름을 모두 확인했습니다.
- [ ] API 키가 코드에 하드코딩되지 않고 환경변수로 분리되어 있습니다.
- [ ] 다른 사람(시크릿 창/다른 기기)이 같은 방법으로 완료 여부를 확인할 수 있습니다.

## 18. 마일스톤 (2일 일정)

| 시점 | 마일스톤 | 확인할 결과 |
|---|---|---|
| 1일차 | 업로드 파이프라인 완성 | 파일을 올리면 Storage와 `documents` 테이블에 저장되고, 검색 화면 뼈대가 존재합니다. |
| 2일차 | AI 검색 기능 완성 + 배포 | 자연어 검색으로 결과가 표시되고, 빈 결과·오류가 안전하게 처리되며, 휴대폰에서 실제 접속이 확인되고 배포가 완료됩니다. |

### 프로젝트 위험과 대응

| 위험 | 예상 영향 | 대응 방법 |
|---|---|---|
| Gemini가 검색 키워드를 부정확하게 추출함 | 원하는 문서를 못 찾음 | 프롬프트에 문서 메타데이터 예시를 포함해 테스트, 필요하면 단순 키워드 매칭을 fallback으로 추가 |
| 샘플 문서 부족으로 검색 테스트가 어려움 | 검색 정확도를 확인할 수 없음 | 1일차에 실제 문서 5~10개를 미리 업로드해둠 |
| Vercel 서버리스의 업로드 용량·시간 제한 | 큰 파일 업로드 실패 | 업로드 파일 크기 제한을 안내하고 Supabase Storage 클라이언트 업로드 방식 사용 |
| 일정 지연으로 문서화 시간 부족 | 제출물(스크린샷, md)을 못 만듦 | 2일차 마지막 1시간을 문서화 전용 버퍼로 고정 |

### 일정 버퍼

- 2일차 후반 1시간: 발견된 버그 수정과 문서화·스크린샷 촬영에 사용

## 19. 이후 작성할 항목

- GitHub README 및 2분 발표자료
