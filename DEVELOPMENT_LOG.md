# javis 개발 로그

이 문서는 미니프로젝트3(javis) 개발 과정을 시작부터 현재까지 시간 순서대로 기록합니다.
의사결정의 배경과 실제로 겪은 문제, 그 원인과 해결 방법을 중심으로 적었습니다.

## 1. 기획 시작

- 이전 수업에서 작성해둔 기획(`jarvis-personal-ai-os`)을 검토함 — 영화 자비스를 모델로 한 "기억·도구제어·안전승인·음성까지 갖춘 개인 AI OS"였는데, 이틀짜리 미니 프로젝트로는 범위가 너무 컸음
- 핵심만 남기고 나머지는 명시적으로 제외하기로 결정: 처음엔 "기억 기반 대화"(사용자가 남긴 메모를 기억하고 질문에 활용) 방향으로 축소
- 이후 실제 논의 중 "찾는 문서를 자동으로 찾아서 전달"이 더 구체적이고 검증 가능한 문제라고 판단해 **"문서 검색·전달"**로 방향을 다시 좁힘 (기획서 v0.1 → v0.2)

## 2. 프로젝트 구조 정리

- 기존 git 저장소가 홈 디렉터리(`/Users/ziho`) 전체를 루트로 잡고 있던 것을 발견 (DineTrue 프로젝트와 섞여 있었음)
- `Desktop/zihoapp`를 별도 프로젝트 폴더로 분리하고 새 git 저장소로 초기화
- GitHub에 `javis` 저장소 새로 생성, Vercel 프로젝트와 연동

## 3. 기본 세팅

- Next.js(App Router, TypeScript, Tailwind) 프로젝트 생성
- Supabase 새 프로젝트(`javis`) 생성 — DineTrue와 데이터 분리
- `documents` 테이블, `documents` Storage 버킷 생성 (초기엔 RLS `public` 정책)
- Gemini API 키를 `.env.local`에 설정 (`.gitignore`로 커밋 방지 확인)

## 4. MVP 구현 (업로드 + 자연어 검색)

- 업로드 화면 + `/api/upload` 라우트(파일을 서버로 보내 Storage에 저장)
- 검색 화면 + `/api/search` 라우트 — 저장된 문서 메타데이터 전체를 프롬프트에 넣고 Gemini에게 자연어 질의와 관련도 높은 문서를 골라달라고 요청하는 방식
- Vercel에 첫 배포 (`javis-mauve.vercel.app`)

## 5. 첫 실사용 버그: 업로드 실패 (413)

- 실제 크기의 문서(건설 CAD 도면 PDF 등)를 업로드하니 실패
- 원인 조사: Vercel 서버리스 함수는 요청 본문이 **4.5MB로 하드 제한**되어 있어, API 라우트를 거치는 업로드 방식 자체가 근본적으로 안 됨
- 해결: 파일을 **브라우저에서 Supabase Storage로 직접 업로드**하도록 변경 (Vercel 함수를 거치지 않음). `/api/upload` 라우트는 삭제
- 부가 수정: 원본 파일명(한글, 공백)을 Storage 키로 쓰면 문제가 생길 수 있어, 저장 경로는 랜덤 UUID + 확장자로 변경

## 6. 연관검색 기능 추가 (여러 차례 방향 수정)

- 처음엔 "위치 기반(GPS)" 검색으로 오해하고 구현하려 했으나, 실제 요구사항은 **"201동처럼 짧은 키워드만 입력하면 AI가 업무를 추론해서 문서를 찾아주는 기능"**이었음 — 재확인 후 방향 수정
- `site_name`(현장·구역명) 컬럼을 `documents` 테이블에 추가
- 1차 구현: 일반 검색과 로직이 거의 같아 "결과가 똑같다"는 피드백 → `site_name` 일치 여부로 후보를 먼저 구조적으로 좁히는 방식으로 개선
- 2차 버그: `site_name`이 비어있는(태깅 안 된) 진짜 관련 문서가 필터에서 통째로 빠짐 → "다른 현장으로 **명확히** 태깅된 문서만 제외"로 필터 로직을 다시 고침 (태그 없는 문서는 후보에 남겨 Gemini가 직접 판단)

## 7. 실제 업무 폴더로 대량 테스트

- 사용자의 실제 업무 폴더(`10_업무_건설건축`) 확인 → 파일 5,044개, 총 21GB → Supabase 무료 플랜 저장 한도(1GB)를 크게 초과
- 대량 업로드용 스크립트(`scripts/bulk-upload.mjs`) 작성 — 폴더를 재귀 탐색, 크기 제한/개수 제한(기본 100개)으로 자름
- 문제 발견: 크기가 작은 파일부터 올리는 방식이라 시스템 잡파일(`desktop.ini`, `~$*.xlsx` 임시파일, `.lnk`, `.ico`)과 **동료들의 실제 서명·도장 이미지**까지 올라감 (개인정보 노출 위험) → 즉시 삭제하고, 확장자 화이트리스트 + 서명/도장 관련 키워드 블랙리스트로 필터 강화
- 문제 발견 2: macOS가 한글 파일명을 분리형(NFD)으로 반환해서, 코드에 적은 결합형(NFC) 키워드("싸인")가 매칭되지 않고 필터를 통과해버림 → 파일명을 NFC로 정규화하는 코드 추가
- 문제 발견 3: 원본 폴더 자체에 같은 파일이 여러 하위 폴더에 중복 저장되어 있어 검색 결과가 중복 도배됨 → 파일명+크기 기준 중복 제거 로직 추가

## 8. 배포 이력 혼란

- 연관검색 등 여러 기능을 로컬에서 만들고 `vercel --prod`로 직접 배포한 적은 있지만, 이후 커밋을 하지 않은 채로 계속 로컬 작업을 이어감
- 사용자가 프로덕션에서 연관검색을 테스트하다 **404**를 만남 → 확인해보니 연관검색 기능은 실제로는 **한 번도 배포된 적이 없었음**이 드러남 (로컬에서만 존재)
- 이 사건 이후 "사용자가 명시적으로 지시하기 전까지는 배포하지 않는다"는 운영 규칙을 확립함

## 9. AI 브리핑 기능

- 검색 결과에서 파일을 열지 않고 버튼 한 번으로 내용을 요약해서 보여주는 기능 추가
- PDF·이미지는 Gemini가 원본 파일을 직접 읽어 요약 (별도 파싱 불필요)
- 스프레드시트(xlsx)는 `exceljs`로 시트 내용을 텍스트로 뽑아 요약하는 것까지 구현했었으나, 아래 8번 보안 사고 이후 **완전히 제외**
- HWP는 `hwp.js`(2020년 이후 업데이트 없는 실험적 라이브러리)로 본문 텍스트를 추출해 요약 — 표·그림 위주 문서는 텍스트가 비어 나와 "지원 안 됨"으로 처리. 일부 `.hwp` 파일이 실제로는 한글 프로그램이 내보낸 HTML이라는 것도 발견해 별도 처리

## 10. 보안 사고와 대응

- xlsx AI 브리핑 테스트 중, 실제 직원 이름·숙소 임차료 금액·계좌 예금주 같은 **민감한 개인정보**가 요약 텍스트에 그대로 노출되는 것을 발견
- 근본 원인 확인: 로그인이 없고, Storage 버킷이 `public`이며, 검색/브리핑 API가 인증 없이 열려 있어서 **URL만 알면 누구나** 실제 회사 문서(급여, 계좌, 이력서 포함)를 검색·열람할 수 있는 상태였음
- 대응:
  1. xlsx는 AI 브리핑 대상에서 완전히 제외 (PDF·이미지만 지원)
  2. 로그인 도입 (아래 11번)
  3. Supabase RLS를 `auth.uid() is not null` 요구로 강화 (documents 테이블 + storage.objects)
  4. Storage 버킷을 `public → private`로 전환, 다운로드 링크를 10분 후 만료되는 서명된 URL로 교체
- 교훈: 로그인 화면(UI)만 막는 것으로는 부족함 — anon key로 직접 API/Storage를 호출하는 경로까지 RLS로 막아야 실제로 안전함을 확인

## 11. 로그인 구현

- 처음엔 Supabase Auth 이메일 가입으로 관리자 계정을 만들려다 `.local`/`-app.com` 도메인이 유효하지 않다고 거부당하고, 여러 번 시도하다 rate limit에 걸림
- 우회: Postgres에 직접 `auth.users`/`auth.identities` row를 삽입해 관리자 테스트 계정(`admin@example.com`) 생성 (`pgcrypto`의 bcrypt 사용). 처음엔 "Database error querying schema" 오류가 나서, `confirmation_token` 등 텍스트 컬럼을 빈 문자열로 채워야 한다는 것을 알아냄
- Google OAuth(Supabase Auth 연동)도 추가 — 처음엔 `redirect_uri_mismatch` 오류가 나서, Google Cloud Console의 승인된 리디렉션 URI를 Supabase 콜백 URL과 정확히 맞추는 것으로 해결
- `middleware.ts`로 미인증 요청을 `/login`으로 리다이렉트 (페이지·API 라우트 모두 적용)
- 로그인 화면에 Google 로그인 버튼과 이메일/비밀번호(테스트 계정) 폼을 함께 제공

## 12. 부가 기능

- **자동 태깅**: 업로드 시 파일명(이미지는 3MB 이하일 때 실제 이미지 내용까지)을 Gemini에게 보내 현장·구역명·설명을 자동으로 채워줌. 사용자가 확인·수정 후 업로드
- **여러 파일 동시 업로드**: 파일을 한꺼번에 선택하면 각각 독립적으로 자동 태깅되고, 검토·수정 후 한 번에 전체 업로드
- **전체 문서 보기**(`/browse`): 검색 없이 저장된 문서 전체를 현장·구역명별로 그룹핑해서 확인

## 13. 현장사진 업로드시 업무 내용 자동생성 + Google Sheets 연동(gemini API)

- 사용자가 "일반 문서 업로드"와 별도로 **현장사진 업로드 버튼**을 추가하고, 사진을 분석 및 추론해서 업무일지 행을 자동 생성하고 싶다고 요청
- 업로드 화면 하단에 **현장사진 업무일지** 영역을 별도로 추가
  - 여러 이미지 파일을 한 번에 선택 가능
  - Gemini가 사진 내용을 보고 `위치`, `업무내용`, `자동 태그`를 추론
  - 사용자가 추론값을 확인·수정한 뒤 한 번에 반영
- 일반 문서 자동 태깅과 구별되는 점:
  - 일반 문서: `documents` 테이블에 검색용 메타데이터 저장
  - 현장사진: 사진 파일은 `documents`에 저장하되, 동시에 `work_logs` 테이블에 업무일지 행을 생성하고 Google Sheets 웹훅에도 전송
- 업무일지 데이터 형태:
  - `date`: 한국 시간 기준 날짜
  - `time`: 한국 시간 기준 시각(`HH:mm:ss`)
  - `location`: 예) `201동 지하 1층`
  - `work_content`: 예) `피복두께 불량 확인 및 시정 조치 후 재확인 및 보고 지시`
  - `tags`: 예) `201동, 지하1층, 철근, 피복두께, 시정지시`
  - `file_name`, `document_id`
- 현재 `documents` 테이블에 별도 `tags` 컬럼은 없으므로, 검색 근거로 쓰이도록 현장사진의 `description`에 `태그: ...`와 업무내용을 함께 저장함
- Google Sheets 연동은 직접 Google Sheets API OAuth를 붙이지 않고, 가장 단순한 **Google Apps Script 웹앱 웹훅** 방식으로 구현
  - 환경변수: `GOOGLE_SHEETS_WEBHOOK_URL`
  - 앱 라우트: `/api/work-log-sheet`
  - 연결 시트: `1GKn9BH_z1SN-eGgLJvjfAZXvpEB8dDbFHS_7YeY3ujQ`
  - 시트 탭 이름: `업무일지`
  - 컬럼: `날짜 | 시간 | 위치 | 업무내용 | 태그 | 파일명 | 문서ID`
- 연결 중 겪은 문제:
  1. 처음 받은 웹앱 URL은 호출됐지만 Google 응답이 `다음 스크립트 함수(doPost)를 찾을 수 없습니다`였음
  2. 원인: 사용자가 Apps Script 코드에서 `doPost(e)`를 `myFunction()` 내부에 중첩 함수로 넣어둠. Apps Script 웹앱은 최상위 `function doPost(e)`만 엔드포인트로 인식함
  3. 해결: `myFunction` 껍데기를 제거하고 최상위에 `doPost(e)`를 둔 뒤, `배포 관리 → 새 버전 → 배포`
  4. 이후 직접 POST 테스트에서 `{"ok": true}` 응답 확인
- 앱 쪽 보완:
  - Google Apps Script가 오류 HTML을 HTTP 200으로 반환할 수 있어서, `/api/work-log-sheet`는 이제 응답 본문이 실제 `{ ok: true }` JSON일 때만 성공 처리함
  - 그렇지 않으면 "Apps Script 배포 확인" 오류를 사용자에게 보여주도록 변경

## 현재 상태 (기획서 v0.9 기준)

- 로컬(`localhost:3000`)에는 위 기능이 전부 구현·테스트되어 있음
- **프로덕션 배포는 보류 중** — 사용자가 명시적으로 지시할 때 진행
- 알려진 한계:
  - 검색·연관검색이 매 요청마다 문서 전체 목록을 프롬프트에 넣는 방식이라, 문서가 수천 개 이상으로 늘어나면 느려지고 비용이 커짐 (벡터 검색으로 전환 필요, 다음 버전 과제)
  - HWP는 표·그림 위주 문서에서 텍스트 추출이 안 됨
  - 로그인은 있지만 멀티유저 권한 분리는 없음 (로그인한 사람은 누구나 같은 문서 전체를 봄)
  - Supabase 무료 플랜 저장 한도(1GB)로 인해 실제 업무 문서 전체(21GB)는 아직 올리지 못함
  - `work_logs` 테이블은 날짜와 내용 중심으로 저장하고, 시트에는 `time`과 `tags`까지 보내는 상태라 DB와 시트의 스키마가 아직 완전히 같지는 않음
  - Google Sheets 연동은 Apps Script 웹앱 URL에 의존하므로, Apps Script를 수정하면 반드시 새 버전으로 다시 배포해야 함
